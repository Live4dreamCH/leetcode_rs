use std::collections::HashMap;
use std::collections::HashSet;

struct Solution;

impl Solution {
    pub fn find_longest_subarray(array: Vec<String>) -> Vec<String> {
        vec![]
    }
}

fn main() {
    Solution::find_longest_subarray(vec![String::from("{{a,z},a{b,c},{ab,z}}")]);
}
